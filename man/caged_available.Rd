% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/available.R
\name{caged_available}
\alias{caged_available}
\title{List the reference months published on the PDET/MTE FTP server}
\usage{
caged_available(year = NULL, timeout = 30, verbose = NULL)
}
\arguments{
\item{year}{Optional integer vector of years to restrict the listing
(for example \code{2025:2026}). \code{NULL} (the default) lists every year since
2020.}

\item{timeout}{Connection timeout in seconds for each FTP request.}

\item{verbose}{Emit progress messages? Defaults to
\code{getOption("cagedr.verbose", TRUE)}.}
}
\value{
A tibble with one row per reference month and columns \code{period}
(integer \code{AAAAMM}), \code{year}, \code{month}, \code{modified} (\code{POSIXct}, folder
modification time on the server) and \code{url} (the folder URL). Sorted
from the most recent to the oldest month. Returns an empty tibble, with
a warning, when the server cannot be reached.
}
\description{
Queries the public FTP server of the Ministry of Labour and Employment and
returns the reference months (\code{AAAAMM}) of the Novo CAGED that are
currently published, with the date each folder was last modified. The
Ministry publishes a reference month around the end of the following
month, and occasionally re-publishes earlier months.
}
\examples{
\donttest{
# Requires network access to ftp.mtps.gov.br
months <- tryCatch(caged_available(year = 2026), error = function(e) NULL)
if (!is.null(months)) head(months)
}
}
\seealso{
\code{\link[=caged_download]{caged_download()}} to fetch the files of a month,
\code{\link[=caged_periods]{caged_periods()}} to build a sequence of months offline.
}
