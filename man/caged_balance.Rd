% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/balance.R
\name{caged_balance}
\alias{caged_balance}
\title{Consolidate admissions, separations and net balance}
\usage{
caged_balance(data, by = NULL)
}
\arguments{
\item{data}{A tibble returned by \code{\link[=caged_read]{caged_read()}} or \code{\link[=caged_fetch]{caged_fetch()}}. Must
contain \code{competenciamov}, \code{saldomovimentacao} and \code{caged_file}.}

\item{by}{Character vector of additional grouping columns (for example
\code{"municipio"}, \code{"uf"}, \code{"secao"}). \code{competenciamov} is always included.}
}
\value{
A tibble with the grouping columns and \code{admissions} (records
with positive sign after the exclusion rule), \code{separations} (negative
sign), \code{net} (\code{admissions - separations}) and \code{records} (number of
records aggregated). When the data contain \code{salario}, also
\code{admission_salary} (sum of the salaries of the admissions) and
\code{mean_admission_salary}.
}
\description{
Applies the accounting rule of the Novo CAGED to a set of records read
with \code{\link[=caged_read]{caged_read()}} or \code{\link[=caged_fetch]{caged_fetch()}} and aggregates them. Movements
declared on time (\code{MOV}) and late (\code{FOR}) count with their own sign;
\strong{exclusions} (\code{EXC}) cancel a previously declared movement, so their
\code{saldomovimentacao} is inverted before summing. The aggregation is always
by \code{competenciamov}, the reference month of the movement, which is what
the Ministry's published totals use.
}
\details{
Because \code{FOR} and \code{EXC} archives of month \emph{M} carry records of earlier
months, the balance of a given month keeps changing as later archives are
published. To reproduce the Ministry's figure for a month, read every
archive published up to the date of interest.
}
\examples{
f <- system.file("extdata", "CAGEDMOV202301_sample.7z", package = "cagedr")
x <- caged_read(f, verbose = FALSE)
caged_balance(x, by = "uf")
}
