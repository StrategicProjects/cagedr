% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/download.R
\name{caged_cache_dir}
\alias{caged_cache_dir}
\title{Cache directory used by cagedr}
\usage{
caged_cache_dir(cache_dir = NULL)
}
\arguments{
\item{cache_dir}{Optional path. When given, it is returned as is (after
creating the folder).}
}
\value{
The cache directory path, created if needed.
}
\description{
Downloaded archives are stored in a local cache so that a reference month
is never downloaded twice. The location is resolved in this order:
}
\details{
\enumerate{
\item the \code{cache_dir} argument;
\item the \code{CAGEDR_CACHE_DIR} environment variable;
\item the \code{cagedr.cache_dir} R option;
\item a session-scoped folder under \code{\link[=tempdir]{tempdir()}}, which R removes when the
session ends.
}

Set one of the first three to keep the archives between sessions. Files
published by the Ministry are large (about 55 MB per month) and change
only when a month is re-published, so a persistent cache is recommended
for repeated work.
}
\examples{
caged_cache_dir()

# For a persistent cache, set the environment variable (for instance in
# your .Renviron). Here a temporary folder is used and the previous value
# is restored afterwards.
old <- Sys.getenv("CAGEDR_CACHE_DIR", unset = NA)
Sys.setenv(CAGEDR_CACHE_DIR = file.path(tempdir(), "caged-cache"))
caged_cache_dir()
if (is.na(old)) Sys.unsetenv("CAGEDR_CACHE_DIR") else Sys.setenv(CAGEDR_CACHE_DIR = old)
}
