% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/download.R
\name{caged_download}
\alias{caged_download}
\title{Download the monthly archives of the Novo CAGED}
\usage{
caged_download(
  period,
  files = c("MOV", "FOR", "EXC"),
  cache_dir = NULL,
  force = FALSE,
  timeout = 900,
  verbose = NULL
)
}
\arguments{
\item{period}{Reference months in \code{AAAAMM} format (integer or character
vector). See \code{\link[=caged_periods]{caged_periods()}} and \code{\link[=caged_available]{caged_available()}}.}

\item{files}{Which files to download: any subset of \code{c("MOV", "FOR", "EXC")}.}

\item{cache_dir}{Optional cache directory (see \code{\link[=caged_cache_dir]{caged_cache_dir()}}).}

\item{force}{Re-download archives already in the cache?}

\item{timeout}{Timeout in seconds for each file. The Ministry's FTP is
slow at times; the default allows 15 minutes per file.}

\item{verbose}{Emit progress messages? Defaults to
\code{getOption("cagedr.verbose", TRUE)}.}
}
\value{
A tibble with one row per (\code{period}, \code{file}) and columns \code{period},
\code{file}, \code{path} (local path, \code{NA} when not available), \code{status}
(\code{"downloaded"}, \code{"cached"}, \code{"not_found"} or \code{"error"}) and \code{url}.
}
\description{
Fetches the \verb{.7z} archives of one or more reference months from the
PDET/MTE FTP server into the local cache (see \code{\link[=caged_cache_dir]{caged_cache_dir()}}).
Archives already in the cache are not downloaded again unless
\code{force = TRUE}.
}
\details{
Every reference month has up to three files, all national:
\itemize{
\item \code{MOV} (\verb{CAGEDMOV<AAAAMM>.7z}): movements declared on time for that
month, the bulk of the data (about 55 MB compressed, 4 to 5 million
records);
\item \code{FOR} (\verb{CAGEDFOR<AAAAMM>.7z}): movements declared \strong{late}, which refer
to earlier reference months (\code{competenciamov < AAAAMM});
\item \code{EXC} (\verb{CAGEDEXC<AAAAMM>.7z}): \strong{exclusions}, movements cancelled by
the employer, also referring to earlier months.
}

The first months of the series (January to March 2020) have no \code{FOR} or
\code{EXC} files; those are reported as \code{not_found}, not as errors.
}
\examples{
\donttest{
# Requires network access; downloads about 1 MB (the EXC file is small).
res <- tryCatch(
  caged_download(202401, files = "EXC", cache_dir = tempdir()),
  error = function(e) NULL
)
res
}
}
\seealso{
\code{\link[=caged_read]{caged_read()}} to read a downloaded archive, \code{\link[=caged_fetch]{caged_fetch()}} for
download and read in one call.
}
