% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/read.R
\name{caged_fetch}
\alias{caged_fetch}
\title{Download and read Novo CAGED microdata in one call}
\usage{
caged_fetch(
  period,
  uf = NULL,
  files = c("MOV", "FOR", "EXC"),
  columns = NULL,
  cache_dir = NULL,
  force = FALSE,
  types = TRUE,
  chunk_size = 500000L,
  timeout = 900,
  verbose = NULL
)
}
\arguments{
\item{period}{Reference months in \code{AAAAMM} format (integer or character
vector). See \code{\link[=caged_periods]{caged_periods()}} and \code{\link[=caged_available]{caged_available()}}.}

\item{uf}{Optional vector of state codes to keep, as IBGE two-digit
numbers (for example \code{26} for Pernambuco, \code{c(26, 25)} for Pernambuco
and Paraiba). \code{NULL} keeps every state.}

\item{files}{Which files to download: any subset of \code{c("MOV", "FOR", "EXC")}.}

\item{columns}{Optional character vector of columns to keep, using the
normalized names listed by \code{\link[=caged_layout]{caged_layout()}}. \code{NULL} keeps all columns.
The \code{uf} column is always read (it is needed for filtering) but is only
returned when requested or when \code{columns} is \code{NULL}.}

\item{cache_dir}{Optional cache directory (see \code{\link[=caged_cache_dir]{caged_cache_dir()}}).}

\item{force}{Re-download archives already in the cache?}

\item{types}{Convert the numeric columns of the layout (codes, salary) to
integer/double? If \code{FALSE} every column is returned as character.}

\item{chunk_size}{Number of lines parsed per chunk. Larger chunks are
faster but use more memory; the default (500,000 lines) uses well under
1 GB.}

\item{timeout}{Timeout in seconds for each file. The Ministry's FTP is
slow at times; the default allows 15 minutes per file.}

\item{verbose}{Emit progress messages? Defaults to
\code{getOption("cagedr.verbose", TRUE)}.}
}
\value{
A tibble with the records of every archive read (see
\code{\link[=caged_read]{caged_read()}} for the columns), or an empty tibble when nothing was
available. The attribute \code{"download"} holds the tibble returned by
\code{\link[=caged_download]{caged_download()}}, so that \code{not_found} and \code{error} files can be
inspected.
}
\description{
Convenience wrapper: \code{\link[=caged_download]{caged_download()}} followed by \code{\link[=caged_read]{caged_read()}} on
every archive found, with the results stacked. Missing files (for
example \code{FOR}/\code{EXC} in early 2020) are skipped with a message.
}
\examples{
\donttest{
# Requires network access. Pernambuco, one month, the three files:
pe <- tryCatch(
  caged_fetch(202401, uf = 26, cache_dir = tempdir(), verbose = FALSE),
  error = function(e) NULL
)
if (!is.null(pe)) table(pe$caged_file)
}
}
