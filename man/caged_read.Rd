% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/read.R
\name{caged_read}
\alias{caged_read}
\title{Read a Novo CAGED archive as a stream}
\usage{
caged_read(
  path,
  uf = NULL,
  columns = NULL,
  types = TRUE,
  chunk_size = 500000L,
  verbose = NULL
)
}
\arguments{
\item{path}{Path to a \code{CAGEDMOV}, \code{CAGEDFOR} or \code{CAGEDEXC} archive, as
returned by \code{\link[=caged_download]{caged_download()}}.}

\item{uf}{Optional vector of state codes to keep, as IBGE two-digit
numbers (for example \code{26} for Pernambuco, \code{c(26, 25)} for Pernambuco
and Paraiba). \code{NULL} keeps every state.}

\item{columns}{Optional character vector of columns to keep, using the
normalised names listed by \code{\link[=caged_layout]{caged_layout()}}. \code{NULL} keeps all columns.
The \code{uf} column is always read (it is needed for filtering) but is only
returned when requested or when \code{columns} is \code{NULL}.}

\item{types}{Convert the numeric columns of the layout (codes, salary) to
integer/double? If \code{FALSE} every column is returned as character.}

\item{chunk_size}{Number of lines parsed per chunk. Larger chunks are
faster but use more memory; the default (500,000 lines) uses well under
1 GB.}

\item{verbose}{Emit progress messages? Defaults to
\code{getOption("cagedr.verbose", TRUE)}.}
}
\value{
A tibble with the selected records and columns, plus two columns
added by the package: \code{caged_file} (\code{"MOV"}, \code{"FOR"} or \code{"EXC"},
detected from the archive name) and \code{caged_period} (the reference month
of the \strong{archive}, which for \code{FOR} and \code{EXC} differs from the
\code{competenciamov} of the records). Column names are normalised: accents
removed and lower case (\code{competenciamov}, \code{municipio}, \code{salario}).
Returns an empty tibble when no record matches.
}
\description{
Reads the \code{.txt} inside a monthly \verb{.7z} archive without extracting it to
disk and without loading the national file in memory: the text is
decompressed as a stream and parsed in chunks, and each chunk is filtered
by state and reduced to the requested columns before being kept. This is
what makes it practical to extract one state (a few hundred thousand
records) from a national file of several million records on a modest
machine.
}
\examples{
# A small sample archive ships with the package (Pernambuco and Bahia rows).
f <- system.file("extdata", "CAGEDMOV202301_sample.7z", package = "cagedr")
x <- caged_read(f, verbose = FALSE)
dim(x)

# One state, a few columns
pe <- caged_read(f, uf = 26,
                 columns = c("competenciamov", "municipio", "saldomovimentacao", "salario"),
                 verbose = FALSE)
pe
}
\seealso{
\code{\link[=caged_layout]{caged_layout()}} for the meaning of every column,
\code{\link[=caged_fetch]{caged_fetch()}} for download and read in one call.
}
